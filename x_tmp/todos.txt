general:
	alles auf deutsch!
	define colore schema for types
	taxonomies
	taxononmies in events
	archive
	single vs. plural folder names
	pagination
	previous/next navigation
	custom js for single pages
	ical entries
	
pressinfo:
	image(s) and scaling on single page.

blog:
	default image in overview if no image there
	archetype for simple / with documents

events:
	default image in overview
	split metadata in base (index.md) and announce
	status:
		planned -> add to overview of planned events (only base meta data filled in)
		announce -> promote on main page (all meta data there)
	clearer naming schema for event documents
	type needed? sbgcup/schcup/other/external (for basic event info only)/norun (sitzung...)
	2 event on same day
	