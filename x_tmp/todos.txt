general:
	archive
	custom js for single pages
	ical entries
	hero
	
pressinfo:
	reduce size of image for web if to big -> open question.

events: (ol event only)
	splitsbrowser link, general: link in result
	define colore schema for different event types

meister:
	layout

{{ if isset $ctx "location"}} in maplink


git:
	make repro private if buddy allows it.
	add colaborators

deployment:
	access to world4you with ssh -> hugo rsync deployment
	https://www.world4you.com/faq/de/webhosting/faq.webspace-via-sshsftp-zugreifen.html
	hugo && rsync -avz --delete public/ www-data@ftp.topologix.fr:~/www/